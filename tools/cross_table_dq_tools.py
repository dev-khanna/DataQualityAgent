"""
tools/cross_table_dq_tools.py

Tools for the cross-table DQ orchestrator agent (e.g. foreign key /
referential integrity checks across tables).

Left empty for now - implemented in a later step, once the single-table
orchestrator is finished.
"""

from langchain.tools import tool
